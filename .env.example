# MoIP Manager Configuration
# Copy this file to .env and fill in your values

# Binary MoIP Controller Settings
MOIP_HOST=192.168.1.100          # IP address of your MoIP controller
MOIP_TELNET_PORT=23              # Telnet port (default: 23)
MOIP_API_PORT=443                # REST API port (default: 443)
MOIP_API_USERNAME=binary         # API username
MOIP_API_PASSWORD=your_password  # API password (check your controller documentation)

# Web Server Settings (optional)
WEB_HOST=0.0.0.0                 # Listen address (0.0.0.0 for all interfaces)
WEB_PORT=8000                    # Web UI port
